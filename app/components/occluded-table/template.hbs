{{#light-table table
               height='90vh'
               occlusion=true
               estimatedRowHeight=50
  as |t|}}

  {{t.head
    fixed=true
  }}

  {{#t.body
    canSelect=false
    scrollBuffer=200
    onScrolledToBottom=(action 'onScrolledToBottom')
  as |body|
  }}
    {{#if isLoading}}
      {{#body.loader}}
        {{table-loader}}
      {{/body.loader}}
    {{/if}}
  {{/t.body}}

  {{t.foot
    fixed=true
  }}
{{/light-table}}
